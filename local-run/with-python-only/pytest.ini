[pytest]
testpaths = tests
markers =
    integration: starts the real solution, needs the full requirements and the extracted viewer
# the integration tests are opt-in, `pytest tests -m integration` runs them
addopts = -m "not integration"
