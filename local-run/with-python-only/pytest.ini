[pytest]
testpaths = tests
markers =
    integration: starts the real solution, needs the full requirements and the extracted viewer
    docker: extracts the real th2-rpt-viewer image, needs podman or docker and ghcr.io access
# both are opt-in, run them with `pytest -m integration` and `pytest -m docker`
addopts = -m "not integration and not docker"
